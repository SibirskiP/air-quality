[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aq-collector-openmeteo"
version = "0.1.0"
dependencies = ["fastapi", "sqlalchemy", "prometheus-client"]